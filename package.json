{
  "name": "abe-qld-owner-builder",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/generate-redirects.mjs && node scripts/check-freshness.mjs",
    "build": "astro build",
    "postbuild": "node scripts/check-redirect-targets.mjs",
    "preview": "astro preview",
    "check": "astro check",
    "lint:prose": "node scripts/prose-lint.mjs",
    "sync:cpd": "node scripts/sync-cpd-register.mjs",
    "prepare": "husky"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/mdx": "^7.0.2",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^7.0.6",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@secretlint/secretlint-rule-preset-recommend": "^13.0.2",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "secretlint": "^13.0.2"
  }
}
