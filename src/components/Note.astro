---
// Note / callout aside. `maroon` = the emphasised left-border variant.
interface Props { maroon?: boolean; }
const { maroon = false } = Astro.props;
---
<p class={`note${maroon ? ' maroon' : ''}`}><slot /></p>
