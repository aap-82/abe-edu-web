---
// Escape hatch for the flexible-build system: the only place free-form HTML from
// a manifest `prose` block lands. Rendered at a readable measure. If the same
// shape keeps landing here, promote it to a real block type + BlockRenderer line.
---
<div class="measure prose"><slot /></div>
